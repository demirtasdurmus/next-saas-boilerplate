// import { db } from '@/libs/db';
// import { examples } from '@/models/schema';

export default async function ExamplePage() {
  // const data = await db
  //   .insert(examples)
  //   .values({
  //     title: 'An example title',
  //     message: 'An example message',
  //   })
  //   .returning();

  return <div>{JSON.stringify({})}</div>;
}
